{
  "name": "@hraness/atet-web",
  "version": "0.0.0",
  "private": true,
  "description": "The static public site for Atet.",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "dependencies": {
    "posthog-js": "catalog:"
  },
  "scripts": {
    "build": "bun run ./scripts/build.ts",
    "check": "bun run test && bun run build",
    "test": "bun test ./site.test.ts"
  }
}
